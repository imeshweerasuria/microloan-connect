const router = require("express").Router();

router.post("/register", (req, res) => res.json({ message: "TODO register (Day 2)" }));
router.post("/login", (req, res) => res.json({ message: "TODO login (Day 2)" }));

module.exports = router;
